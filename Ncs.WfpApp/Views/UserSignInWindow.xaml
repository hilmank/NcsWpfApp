<Window x:Class="Ncs.WfpApp.Views.UserSignInWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ncs.WfpApp.Views"
        mc:Ignorable="d"
        Title="UserSignInWindow" Height="300" Width="400">
    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="Username:"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Text="Password:" Margin="0,10,0,0"/>
            <PasswordBox x:Name="PasswordBox"
                         PasswordChanged="PasswordBox_PasswordChanged"/>

            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red"/>

            <Button Content="Sign In" Command="{Binding SignInCommand}" Margin="0,20,0,0"/>
        </StackPanel>
    </Grid>
</Window>
